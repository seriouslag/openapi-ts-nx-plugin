{
  "executors": {
    "update-api": {
      "implementation": "./dist/updateApi.cjs.js",
      "schema": "./dist/updateApi.json",
      "description": "Updates the OpenAPI spec file and generates new client code if needed."
    }
  }
}
